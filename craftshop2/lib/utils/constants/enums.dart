/// list of enums
/// connot be created inside a class
///
enum TextSizes{
  small,
  base,
  medium,
  large,
}

enum OrderStatus{
  pending,
  processing,
  shipped,
  delivered,
  cancelled
}

enum PaymentMethod { paypal, creditCard, cashOnDelivery , googlePay, applePay, visa, masterCard, momo, appBanking }